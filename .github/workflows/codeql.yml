# .github/workflows/codeql.yml
name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '24 4 * * 6'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    # Use standard ubuntu runner for Rust
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
        # CHANGED: Removed javascript-typescript, kept only rust
        - language: rust
          build-mode: none 

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive

    # --- START OF RUST ENVIRONMENT SETUP FOR CODEQL ---
    # This step ensures that all necessary targets are available for CodeQL's internal build.
    - if: matrix.language == 'rust'
      name: Prepare Rust environment for CodeQL
      shell: bash
      run: |
        echo "Preparing Rust environment for CodeQL analysis..."
        rustup update --no-self-update stable
        rustup default stable
        
        # Install targets for cross-platform support during analysis (fixes compilation checks)
        rustup target add aarch64-unknown-linux-gnu x86_64-unknown-linux-musl || true
        echo "Rust environment preparation complete."
    # --- END OF RUST ENVIRONMENT SETUP FOR CODEQL ---

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}
      env:
        # Pass release flag to CodeQL build
        CODEQL_ACTION_EXTRA_OPTIONS: >-
          ${{ matrix.language == 'rust' && '--release' || '' }}

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"